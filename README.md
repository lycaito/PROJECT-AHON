
  # Upload UI to GitHub

  This is a code bundle for Upload UI to GitHub. The original project is available at https://www.figma.com/design/YI5hZStzlBGYtnwEUv0l8S/Upload-UI-to-GitHub.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  